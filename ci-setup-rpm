#!/bin/sh

set -ev

# always make sure we have no random rpm around
removerpmdeps 0 || true

if [ "$build" = rpm ]; then
 if [ -n "$RPMDEPS" ]; then
  echo "Installing rpm deps: $RPMDEPS"
  installrpmdeps
 else
  echo "No rpm deps requested"
 fi
fi
