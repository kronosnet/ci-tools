name: ksan-registry
docker_user: ${docker_user}
docker_password: ${docker_password}
client: kubevirt
ipv6: false
vmrules:
- ksan-registry:
   nets:
   - name: default
     masquerade: true
   memory: 2048
   numcpus: 2
